<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Soltec.NET.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Soltec.NET"
    Title="Soltec.NET">

    <!-- Página principal -->
    <FlyoutItem Title="Inicio" Icon="home.png">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <!-- Otra página -->
    <FlyoutItem Title="Pañol" Icon="settings.png">
        <ShellContent ContentTemplate="{DataTemplate local:PanolPage}" />
    </FlyoutItem>

    <!-- Otra página -->
    <FlyoutItem Title="Manuales" Icon="settings.png">
        <ShellContent ContentTemplate="{DataTemplate local:ManualesPage}" />
    </FlyoutItem>

    <!-- Otra página -->
    <FlyoutItem Title="Politicas" Icon="settings.png">
        <ShellContent ContentTemplate="{DataTemplate local:PoliticasPage}" />
    </FlyoutItem>

    <!-- Otra página -->
    <FlyoutItem Title="Planos" Icon="settings.png">
        <ShellContent ContentTemplate="{DataTemplate local:PlanosPage}" />
    </FlyoutItem>

    <!-- Otra página -->
    <FlyoutItem Title="Calculo Motores" Icon="settings.png">
        <ShellContent ContentTemplate="{DataTemplate local:MotoresPage}" />
    </FlyoutItem>

</Shell>
